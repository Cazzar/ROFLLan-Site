<h1><%= @pagetitle %></h1>

<% @posts.each do |post| %>
  <div name="posts">
    <b><%= link_to "#{post.title} - By #{post.user.username}", post %></b>
    <%= raw(GitHub::Markdown.render_gfm(post.data.truncate(200))) %>
  </div>
<% end %>


<%= will_paginate @posts %>