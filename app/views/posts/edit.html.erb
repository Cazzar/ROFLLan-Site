<%= form_for :post, url: @post, method: :patch do |f| %>
	<% if @post.errors.any? %>
	<div id="error_explanation">
		<h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved: </h2>
		<% @post.errors.full_messages.each do |msg| %>
      		<li><%= msg %></li>
    	<% end %>
	</div>
	<% end %>

	<p>
        <%= f.label :title %> <br>
        <%= f.text_field :title %>
    </p>
    <p>
        <%= f.label :data %> <br>
        <%= f.text_area :data %>
    </p>
    <p>
        <%= f.submit %>
    </p>
<% end %>

<%= link_to 'Back', @post %>